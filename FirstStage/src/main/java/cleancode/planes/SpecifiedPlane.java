package cleancode.planes;

public class SpecifiedPlane extends Plane {
    public SpecifiedPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
